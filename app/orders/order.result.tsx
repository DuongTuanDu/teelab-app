import { IOrder } from '@/redux/order/order.interface'
import React from 'react'
import { View } from 'react-native'

interface IProps {
    order: IOrder
}
const OrderResult = ({ order }: IProps) => {
    return (
        <View>

        </View>
    )
}

export default OrderResult
